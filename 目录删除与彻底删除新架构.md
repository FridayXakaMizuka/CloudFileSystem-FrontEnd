# 回收站系统架构设计文档

## 1. 概述

本文档描述了基于 Redis 与 MySQL 混合架构的回收站系统设计，涵盖文件删除、恢复及过期清理的完整业务流程。系统采用异步处理、状态机驱动和限流控制等技术，确保高并发场景下的数据一致性和系统稳定性。

## 2. 架构设计目标

- **高性能**：前端回收站操作响应时间 < 100ms
- **高并发**：支持每秒 1000+ 次回收站操作
- **数据一致性**：确保 Redis 缓存与 MySQL 数据最终一致
- **可扩展性**：支持海量文件存储场景
- **用户体验**：提供流畅的删除/恢复操作体验

## 3. 技术架构

### 3.1 整体架构图

```
+----------------+     +-----------------+     +-------------------+  
|   前端界面      |<--->|   API网关      |<--->|   业务服务层      |  
+----------------+     +-----------------+     +-------------------+  
                                                |  - 删除服务       |  
                                                |  - 恢复服务       |  
                                                |  - 清理服务       |  
                                                +-------------------+  
                                                          |  
                                                          v  
                                          +----------------------------+  
                                          |   Redis缓存集群           |  
                                          |   - 回收站列表(ZSet)      |  
                                          |   - 节点详情(Hash)        |  
                                          |   - 恢复队列(List)         |  
                                          |   - 限流器(ZSet)           |  
                                          +----------------------------+  
                                                          |  
                                                          v  
                                          +----------------------------+  
                                          |   MySQL数据库             |  
                                          |   - 文件表(file_table)    |  
                                          |   - 文件夹表(folder_table)|  
                                          |   - 回收站任务表(task_table)|  
                                          +----------------------------+  
```

### 3.2 核心组件

#### 3.2.1 Redis 缓存层

- **回收站列表**：`recycle:list:{user_id}` (ZSet)
- **节点详情**：`recycle:node:{node_id}` (Hash)
- **恢复队列**：`restore:queue` (List)
- **限流控制**：`rate_limit:{type}` (ZSet)

#### 3.2.2 MySQL 持久层

- **文件表**：存储文件元数据
- **文件夹表**：存储文件夹元数据
- **回收站任务表**：记录删除批次信息

## 4. 删除操作流程

### 4.1 流程概述

1. 用户选择文件/文件夹进行删除
2. 系统生成唯一 `batch_id`
3. 递归扫描子节点并写入 Redis
4. 更新 MySQL 状态为 `DELETED`
5. 返回删除成功响应

### 4.2 详细流程

#### 4.2.1 请求处理

#### 4.2.2 Redis 数据结构

**回收站列表 (ZSet)**

- **Key**: `recycle:list:{user_id}`
- **Member**: `node_id`
- **Score**: `delete_timestamp`

**节点详情 (Hash)**

- **Key**: `recycle:node:{node_id}`
- **Fields**:
  - `type`: folder/file
  - `name`: 节点名称
  - `size`: 文件大小
  - `ext`: 文件扩展名
  - `batch_id`: 删除批次 ID
  - `parent_id`: 原父节点 ID

### 4.3 性能优化

- **Pipeline 批量操作**：减少 Redis 网络往返
- **异步写入**：删除操作后异步更新统计信息
- **限流控制**：防止大目录删除影响系统稳定性

## 5. 恢复操作流程

### 5.1 流程概述

1. 用户选择回收站项进行恢复
2. 校验 `batch_id` 一致性
3. 状态校验（非 `RESTORING` 状态）
4. 加入恢复队列
5. 异步执行恢复操作

### 5.2 详细流程

#### 5.2.1 状态校验

```java
public RestoreResult restoreNode(String batchId, Long nodeId) {
    // 1. 校验节点是否存在
    String nodeKey = "recycle:node:" + nodeId;
    if (!redis.exists(nodeKey)) {
        throw new NodeNotFoundException();
    }
    
    // 2. 校验batch_id一致性
    String nodeBatchId = redis.hget(nodeKey, "batch_id");
    if (!nodeBatchId.equals(batchId)) {
        throw new BatchIdMismatchException();
    }
    
    // 3. 检查状态（乐观锁）
    FileNode node = fileMapper.selectById(nodeId);
    if (node.getStatus() == Status.RESTORING) {
        throw new NodeRestoringException();
    }
    
    // 4. 加入恢复队列
    redis.rpush("restore:queue", nodeId.toString());
    
    return RestoreResult.success();
}
```

#### 5.2.2 异步恢复 Worker

```java
@Component
public class RestoreWorker {
    
    @Scheduled(fixedRate = 100)
    public void processRestoreQueue() {
        // 1. 限流检查
        if (!rateLimiter.isAllowed("restore", 50, 1000)) {
            return;
        }
        
        // 2. 消费队列
        String nodeIdStr = redis.lpop("restore:queue");
        if (nodeIdStr == null) {
            return;
        }
        
        Long nodeId = Long.valueOf(nodeIdStr);
        
        try {
            // 3. 执行恢复
            fileService.restoreNode(nodeId);
            
            // 4. 更新进度
            updateRestoreProgress(nodeId);
            
        } catch (Exception e) {
            // 5. 失败处理
            handleRestoreFailure(nodeId, e);
        }
    }
}
```

### 5.3 恢复状态管理

**恢复进度追踪**

- **Key**: `restore:progress:{batch_id}`
- 使用 ZSet 记录已恢复节点
- 前端通过轮询获取恢复进度

## 6. 彻底删除与过期清理

### 6.1 过期机制设计

#### 6.1.1 触发器设计

- **Key**: `recycle:expire_trigger:{batch_id}`
- **Value**: `"1"`
- **TTL**: 30 天（与回收站保留策略一致）

#### 6.1.2 调度器设计

```java
@Component
public class ExpireScheduler {
    
    @Scheduled(fixedRate = 60000) // 每分钟执行
    public void checkExpiredTriggers() {
        // 1. 扫描过期触发器
        Set<String> expiredKeys = redis.keys("recycle:expire_trigger:*");
        
        for (String key : expiredKeys) {
            // 2. 检查是否真正过期
            if (redis.ttl(key) <= 0) {
                String batchId = extractBatchId(key);
                
                // 3. 加入清理队列
                cleanupQueue.add(batchId);
            }
        }
    }
}
```

### 6.2 彻底删除流程

#### 6.2.1 清理 Worker

```java
@Component
public class CleanupWorker {
    
    @Scheduled(fixedRate = 200)
    public void processCleanupQueue() {
        // 1. 限流检查
        if (!rateLimiter.isAllowed("cleanup", 100, 1000)) {
            return;
        }
        
        // 2. 获取待清理批次
        String batchId = cleanupQueue.poll();
        if (batchId == null) {
            return;
        }
        
        try {
            // 3. 获取批次下所有节点
            List<Long> nodeIds = getNodeIdsByBatch(batchId);
            
            // 4. 分批更新状态
            for (Long nodeId : nodeIds) {
                fileMapper.updateStatus(nodeId, Status.PENDING_ALLOCATION);
            }
            
            // 5. 清理Redis缓存
            cleanRedisCache(batchId);
            
        } catch (Exception e) {
            // 6. 记录失败日志
            log.error("Cleanup failed for batch: {}", batchId, e);
        }
    }
}
```

#### 6.2.2 Redis 缓存清理

```java
private void cleanRedisCache(String batchId) {
    // 1. 获取批次下所有节点
    Set<String> nodeIds = redis.zrange("recycle:list:" + userId, 0, -1);
    
    // 2. 删除节点详情
    for (String nodeId : nodeIds) {
        redis.del("recycle:node:" + nodeId);
    }
    
    // 3. 删除列表项
    redis.zrem("recycle:list:" + userId, nodeIds.toArray());
    
    // 4. 删除触发器
    redis.del("recycle:expire_trigger:" + batchId);
}
```

### 6.3 兜底机制

#### 6.3.1 定时补偿任务

```java
@Component
public class CompensationTask {
    
    @Scheduled(cron = "0 0 * * *") // 每小时执行
    public void compensateExpiredItems() {
        // 1. 查询已过期但未清理的项
        List<RecycleTask> expiredTasks = taskMapper.findExpiredTasks();
        
        for (RecycleTask task : expiredTasks) {
            // 2. 重新加入清理队列
            cleanupQueue.add(task.getBatchId());
        }
    }
}
```

## 7. 限流控制设计

### 7.1 滑动窗口限流器

```java
public class SlidingWindowRateLimiter {
    
    public boolean isAllowed(String key, int maxCount, int windowMs) {
        long now = System.currentTimeMillis();
        long windowStart = now - windowMs;
        
        // 1. 移除窗口外的记录
        redis.zremrangebyscore(key, 0, windowStart);
        
        // 2. 统计当前窗口内请求数
        Long currentCount = redis.zcard(key);
        
        if (currentCount >= maxCount) {
            return false;
        }
        
        // 3. 添加当前请求
        redis.zadd(key, now, UUID.randomUUID().toString());
        
        // 4. 设置过期时间
        redis.expire(key, windowMs / 1000);
        
        return true;
    }
}
```

### 7.2 限流策略

| 操作类型 | 限流 Key | 最大 QPS | 窗口时间 |
|---------|---------|---------|---------|
| 恢复操作 | `restore` | 50 | 1 秒 |
| 清理操作 | `cleanup` | 100 | 1 秒 |
| 删除操作 | `delete` | 200 | 1 秒 |

## 8. 数据一致性保障

### 8.1 最终一致性设计

1. **写入顺序**：先写 Redis，再写 MySQL
2. **失败回滚**：Redis 写入失败则不执行 MySQL 操作
3. **补偿机制**：定时任务修复不一致数据

### 8.2 事务边界控制

- **删除操作**：Redis Pipeline + MySQL 事务
- **恢复操作**：状态校验 + 乐观锁
- **清理操作**：分批处理 + 限流控制

## 9. 监控与告警

### 9.1 监控指标

| 指标名称 | 采集方式 | 告警阈值 |
|---------|---------|---------|
| 恢复队列长度 | Redis ZCARD | > 1000 |
| 清理队列长度 | Redis LLEN | > 500 |
| Redis 内存使用 | INFO memory | > 80% |
| MySQL 连接数 | SHOW STATUS | > 90% |

### 9.2 日志记录

- **操作日志**：记录所有删除/恢复操作
- **错误日志**：记录失败的清理任务
- **性能日志**：记录关键操作耗时

## 10. 性能优化

### 10.1 缓存优化

- **本地缓存**：热点数据使用 Caffeine 缓存
- **Redis 集群**：分片存储，避免单点瓶颈
- **连接池**：使用 Lettuce 连接池

### 10.2 异步处理

- **消息队列**：使用 Kafka 处理非实时任务
- **线程池**：合理配置线程池大小
- **批量处理**：合并小操作为大批次

## 11. 安全设计

### 11.1 权限控制

- **用户隔离**：每个用户只能操作自己的回收站
- **数据校验**：防止越权访问
- **操作审计**：记录所有敏感操作

### 11.2 防攻击设计

- **限流保护**：防止 DDoS 攻击
- **输入校验**：防止 SQL 注入
- **接口鉴权**：所有接口需要 Token 验证

## 12. 总结

本架构设计通过 Redis 与 MySQL 的混合使用，实现了高性能、高可用的回收站系统。核心优势包括：

1. **高性能**：Redis 缓存大幅提升读写性能
2. **高并发**：异步处理和限流控制保障系统稳定性
3. **数据一致**：完善的补偿机制确保最终一致性
4. **用户体验**：流畅的删除/恢复操作体验

该设计已考虑海量文件场景下的各种边界情况，可直接用于生产环境。

---

