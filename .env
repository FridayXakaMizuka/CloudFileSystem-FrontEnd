# 后端服务器配置
# 默认使用 localhost，局域网访问时需要修改为实际 IP

# 后端主机地址
# - localhost: 仅本机访问
# - 0.0.0.0: 监听所有网络接口（需要后端也配置为 0.0.0.0）
# - 192.168.x.x: 指定具体的 IP 地址（局域网访问）
# 注意：局域网访问时，请改为你电脑的局域网 IP 地址
VITE_BACKEND_HOST=192.168.31.187

# 后端端口号
VITE_BACKEND_PORT=8835
