# 企业级网盘异步删除与恢复架构设计文档
## 1. 架构概述与设计目标
本架构旨在解决网盘系统在大规模文件删除与恢复场景下，因数据库 I/O 飙升导致的系统阻塞与用户体验下降问题。核心设计目标包括：
-	极致用户体验：前端操作毫秒级响应，消除等待焦虑。
-	数据库绝对安全：通过平滑限流与事务拆分，将数据库 I/O 控制在安全阈值内，避免长事务与死锁。
-	高并发一致性：通过乐观锁与状态前置校验，彻底解决异步清理任务与用户前台操作的并发冲突。
## 2. 核心架构设计
### 2.1 异步任务解耦与队列驱动
将用户的“删除/恢复操作”与底层的“状态变更/物理清理”彻底解耦。
-	前端交互层：用户触发操作后，后端仅对顶层目录进行状态标记（如标记为“删除中”），并立即返回成功响应。
-	消息队列层：采用 Redis 作为轻量级任务队列，仅存储待处理的顶层目录 ID。
-	后台消费层：独立的 Worker 节点从队列中消费任务，执行子文件的批量状态变更与物理清理。
### 2.2 滑动窗口/令牌桶平滑限流机制
废弃原有的“定时清零”脉冲式限流，采用
**滑动窗口（Sliding Window）或令牌桶（Token Bucket**
算法，实现 I/O 的绝对平滑。
-	实现方案：基于 Redis 的有序集合（ZSet）实现滑动窗口。
-	执行逻辑：每次执行数据库 I/O 操作前，先通过 Lua 脚本清理窗口外的过期时间戳，统计当前窗口内的请求数。若未超阈值，则插入当前时间戳并放行；若超阈值，则 Worker 进入短暂休眠（Sleep）后重试。
-	效果：将数据库负载从“锯齿状”削峰为“平滑直线”，彻底杜绝瞬间打满数据库连接池的风险。
### 2.3 事务拆分与批量处理策略
严禁在后台清理时执行无限制的批量  或 ，采用“小步快跑”策略。
-	分批处理：单次批量更新/删除的记录数严格控制在 1000~5000 条以内。
-	事务隔离：每批次操作作为一个独立事务提交。提交后，结合滑动窗口限流器检查 IOPS，必要时主动休眠（如 50ms~100ms），避免产生海量 Undo/Redo Log 及主从复制延迟。
## 3. 并发控制与数据一致性保障
### 3.1 状态前置校验与快速失败（Fail-Fast）
在用户执行删除、重命名、移动等操作时，后端首先进行状态拦截：
-	三向校验：对于移动操作，必须同时校验“源父目录”、“目标父目录”及“被操作实体”的状态。
-	快速失败：只要上述任一实体的状态非 （如处于“删除中”、“已删除”），立即拦截请求并返回明确的 ERROR 提示（如“文件正在被系统清理或已被删除”），避免进入数据库层产生行锁竞争。
### 3.2 数据库乐观锁（Optimistic Locking）兜底
为彻底解决“检查与执行（Check-Then-Act）”之间的毫秒级竞态条件，在数据库层面引入乐观锁机制。
-	表结构设计：在文件/文件夹元数据表中增加 （版本号）字段。
-	安全更新：用户的  语句必须携带版本条件，例如：。
-	冲突处理：若后台清理任务刚好修改了该记录，用户的 SQL 影响行数将为 0。后端捕获到影响行数为 0，即可抛出与前端相同的 ERROR 提示，实现绝对的数据安全。
## 4. 异常兜底与补偿机制
-	孤儿数据清理：Redis 队列不作为唯一依赖。系统需配备独立的定时巡检任务（如每日凌晨低峰期），扫描数据库中  的孤儿数据，重新投入清理队列，防止因 Redis 宕机或异常导致的数据残留。
-	非核心元数据延迟重建：恢复或删除操作仅更新核心状态字段。对于全文检索索引、缩略图缓存等非核心元数据，通过监听 Binlog 或消息队列在后台异步重建，进一步降低主流程的 I/O 负担。
## 5. 架构收益总结
本架构通过队列解耦实现了用户操作的秒级响应；通过滑动窗口限流与事务拆分保障了数据库在海量数据清理下的绝对稳定；通过前置校验与乐观锁构建了无懈可击的并发安全防线。该方案兼顾了极致的用户体验与企业级的系统鲁棒性，可完美支撑千万级文件目录的删除与恢复场景。

